<%= link_to "Back", admin_orders_path, class: "btn btn-light" %>
<%= link_to "Edit", edit_admin_order_path(@order), class: "btn btn-warning" %>

<% if @fx_signals.any? %>
  <div class="divider" />
  <h5>Forex Signals</h5>
  <div class="divider" />

  <%= render "admin/fx_signals/fx_signals" %>
<% end %>

<% if @positions.any? %>
  <div class="divider" />
  <h5>Positions</h5>
  <div class="divider" />

  <%= render "admin/positions/positions" %>
<% end %>
